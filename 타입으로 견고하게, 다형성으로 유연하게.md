# 타입으로 견고하게, 다형성으로 유연하게

> 정적 타입 시스템과 다형성 완벽 가이드

프로그래밍에서 **타입 시스템**은 버그를 줄이고 코드의 안정성을 확보하는 핵심 요소입니다. 특히 버그의 가장 흔한 원인 중 하나가 타입 에러이기 때문에, 타입 검사기는 현대 언어의 필수불가결한 부분이 되었습니다.

---

## 📌 목차

1. [정적 타입 검사의 양날의 검](#정적-타입-검사의-양날의-검)
2. [다형성의 세 가지 형태](#다형성의-세-가지-형태)
   - [서브타입에 의한 다형성](#1-서브타입에-의한-다형성)
   - [매개변수에 의한 다형성 (제네릭)](#2-매개변수에-의한-다형성-제네릭)
   - [가변성 (Variance)](#3-가변성-variance)
   - [오버로딩과 오버라이딩](#4-오버로딩과-오버라이딩)

---

## 정적 타입 검사의 양날의 검

타입 검사기를 사용하는 것은 개발자가 아닌 언어를 제작하는 개발자의 몫이지만, 완벽한 타입 검사기는 존재하지 않으므로 우리는 **타입 시스템의 원칙과 한계**를 잘 이해해야 합니다.

### 타입 검사기의 한계

정적 타입 언어에서 타입 표시는 '양날의 검'과 같습니다.

#### ⚠️ 거짓 긍정 (False Positive)

타입 검사기가 틀린 말을 하는 경우입니다. 개발자가 보기에는 코드가 완벽히 올바르지만, 타입 검사기가 '타입 에러'라며 코드를 거부하는 경우를 말합니다.

💡 **결과**: 프로그래머가 코드의 정확성을 증명하기 위해 불필요한 코드를 추가하게 만들 수 있습니다.

#### ❌ 타입 검사 원칙 파괴 (Break Type Safety)

타입 검사를 무력화하는 위험한 기능을 사용하는 경우입니다. 대표적인 예로 **코틀린의 `!!` 연산자**가 있습니다.

```kotlin
val str: String = nullableValue!! // 위험: NullPointerException 가능
```

💡 **문제점**: 개발자는 이 코드가 에러를 발생시키지 않을 것이라고 단정하고 `!!`를 사용하지만, 이는 타입 검사기가 널(Null) 값을 String 타입의 값으로 강제하도록 만들어 실제 런타임에서 `NullPointerException`이 발생할 위험을 만듭니다.

---

## 다형성의 세 가지 형태

**다형성(Polymorphism)**은 '많은 형태를 가진다'는 의미로, 하나의 코드가 여러 타입을 처리할 수 있도록 하는 개념입니다.

### 다형성의 분류

| 다형성 종류 | 영문명 | 핵심 개념 |
|------------|--------|----------|
| **서브타입 다형성** | Subtype Polymorphism | 상속 관계를 이용한 타입 호환 |
| **매개변수 다형성** | Parametric Polymorphism | 제네릭을 이용한 타입 추상화 |
| **오버로딩 다형성** | Ad-hoc Polymorphism | 메서드 이름 재사용 |

---

## 1. 서브타입에 의한 다형성

클래스나 인터페이스의 **상속 관계**를 이용합니다. 서브타입 객체는 슈퍼타입이 필요한 모든 곳에서 사용될 수 있습니다 (**리스코프 치환 원칙**).

### 이름에 의한 서브타입 (Nominal Subtyping)

타입 검사기는 **클래스의 이름**과 **클래스 사이의 상속 관계**만 고려합니다.

✅ **규칙**: 클래스 A가 클래스 B를 상속한다면 (직접 또는 간접), A는 B의 서브타입입니다.

**Java 예시:**

```java
class Person {
    String email;
}

class Student extends Person {
    int grade;
}

void sendEmail(Person person) {
    String email = person.email;
    // ...
}

Student st = new Student();
sendEmail(st); // ✅ Student는 Person의 서브타입이므로 통과
```

**Kotlin 예시:**

```kotlin
open class Person(val email: String)

class Student(email: String, val grade: Int) : Person(email)

fun sendEmail(person: Person) {
    val email: String = person.email
    // ...
}

val st: Student = Student("test@mail.com", 1)
sendEmail(st) // ✅ Student 객체를 Person 자리에 전달 가능
```

### 구조에 의한 서브타입 (Structural Subtyping)

이름에 의한 서브타입이 부족할 때 사용됩니다. 특히 **이미 정의된 클래스**(라이브러리 클래스 등)를 수정할 수 없어 상속 관계를 만들 수 없을 때 유용합니다.

✅ **규칙**: 타입 검사기는 객체가 가지는 **필드와 메서드의 구조**가 일치하는지 확인합니다.

#### 문제 상황

Person과 Student 클래스가 별개의 라이브러리에 정의되어 있어 상속 관계를 만들 수 없는 경우:

```java
// 라이브러리 A
class Person {
    String name;
    String email;
    int id;
}

// 라이브러리 B
class Student {
    String name;
    String email;
    int grade;
    float gpa;
    boolean paid;
    // Person을 상속할 수 없음!
}
```

#### 해결책 1: 중간 클래스 생성

```java
// HasEmail 클래스를 새로 정의
class HasEmail {
    String email;
}

// sendEmail이 HasEmail 타입을 요구하게 변경
void sendEmail(HasEmail he, String title, String content) {
    String email = he.email;
    // ...
}
```

💡 **문제점**: 여러 번 사용하지 않는 `HasEmail` 클래스를 굳이 정의해야 할까?

#### 해결책 2: 구조를 드러내는 타입 (개념적)

```typescript
// TypeScript 스타일 구조 타입
void sendEmail({String email;} person, String title, String content) {
    String email = person.email;
    // ...
}
```

⚠️ **참고**: 이 문법은 자바/코틀린 표준이 아닌, TypeScript 같은 언어에서 주로 사용되는 개념입니다.

### 추상 메서드와 최대 타입

#### 추상 메서드 (Abstract Method)

**몸통 없이** 이름, 매개변수 타입, 결과 타입만 정의된 메서드를 **메서드 시그니처**라고 합니다. 이를 포함하는 추상 클래스나 인터페이스는 서브타입 다형성의 기반이 됩니다.

**Java 예시:**

```java
abstract class EmailDst {
    abstract void sendEmail(String content);
}

class Person extends EmailDst {
    @Override
    void sendEmail(String content) { /* ... */ }
}

void sendNewYearEmail(EmailDst dst) {
    dst.sendEmail("Happy New Year!");
}

Person pr = new Person();
sendNewYearEmail(pr); // ✅ Person은 EmailDst의 서브타입
```

**Kotlin 예시:**

```kotlin
abstract class EmailDst {
    abstract fun sendEmail(content: String): Unit
}

class Person : EmailDst() {
    override fun sendEmail(content: String): Unit { /* ... */ }
}

fun sendNewYearEmail(dst: EmailDst): Unit {
    dst.sendEmail("Happy New Year!")
}

val pr: Person = Person()
sendNewYearEmail(pr)
```

#### 최대 타입 (Top Type)

**모든 타입의 슈퍼타입**입니다.

| 언어 | 최대 타입 | 널 포함 |
|------|----------|---------|
| Java | `Object` | - |
| Kotlin | `Any` | `Any?` (널러블 포함) |

**Java 예시:**

```java
Object res = true ? 1 : false; // int 또는 boolean
```

**Kotlin 예시:**

```kotlin
val res: Any? = if (true) 1 else false // Int 또는 Boolean
```

### 이면서 타입 (Intersection Type)

두 가지 이상의 타입을 **동시에** 만족하는 객체를 요구할 때 사용됩니다.

```java
// Student와 Teacher 타입을 모두 만족하는 객체를 받습니다.
Float getGrade(Student & Teacher st) {
    // st는 Student의 메서드와 Teacher의 메서드를 모두 사용 가능
    return st.grades.get(st.course);
}
```

💡 **비교**: 이거나 타입 (`Student | Teacher`)은 둘 중 하나만 만족하면 되지만, 이면서 타입 (`Student & Teacher`)은 둘 다 만족해야 합니다.

### 함수 타입과 서브타입

함수도 일급 시민(First-Class Citizen)으로 값처럼 사용할 수 있습니다.

**핵심 규칙**: 함수 타입은 **매개변수 타입의 서브타입 관계를 뒤집고**, **결과 타입의 서브타입 관계를 유지**합니다.

**Java 예시:**

```java
class Person {}
class Student extends Person {}

void startMentoring(Function<Student, Person> select) {
    Student st = /* ... */;
    Person mentor = select.apply(st);
    // ...
}

class A {
    static Student selectStudentMentor(Student st) { /* ... */ }
}

// Function<Student, Student>는 Function<Student, Person>의 서브타입
startMentoring(A::selectStudentMentor); // ✅ 통과
```

**Kotlin 예시:**

```kotlin
open class Person
class Student : Person()

fun startMentoring(select: (Student) -> Person): Unit {
    val st: Student = /* ... */
    val mentor: Person = select(st)
    // ...
}

fun selectStudentMentor(st: Student): Student = /* ... */

// (Student) -> Student는 (Student) -> Person의 서브타입
startMentoring(::selectStudentMentor) // ✅ 통과
```

---

## 2. 매개변수에 의한 다형성 (제네릭)

함수나 타입 내부에 **타입 매개변수 (Type Parameter)**를 추가하여 다형성을 얻습니다.

### 제네릭의 필요성

모든 타입에 대해 동일하게 동작하는 `choose` 함수를 예로 들어 봅시다.

```java
String choose(String v1, String v2) {
    print(v1); print(v2);
    int input = readInt();
    return (input == 0) ? v1 : v2;
}
```

💡 **문제**: 정수를 선택하고 싶다면? 다른 타입은?

| 접근 방법 | 평가 | 문제점 |
|----------|------|--------|
| **해결책 1**: 타입마다 함수 작성 | ❌ | 코드 중복, 유지보수 어려움 |
| **해결책 2**: `Any` 타입 사용 | ⚠️ | 반환 타입 정보 손실, 타입 안전성 저하 |
| **해결책 3**: 제네릭 사용 | ✅ | 타입 안전 + 코드 재사용 |

```java
// ❌ 해결책 1: 코드 중복
String choose(String v1, String v2) { /* ... */ }
Integer chooseInt(Integer v1, Integer v2) { /* ... */ }

// ⚠️ 해결책 2: 타입 정보 손실
Object choose(Object v1, Object v2) {
    return (input == 0) ? v1 : v2;
}

// ✅ 해결책 3: 제네릭
<T> T choose(T v1, T v2) {
    return (input == 0) ? v1 : v2;
}
```

### 제네릭 메서드

**Java 예시:**

```java
class A {
    static <T> T choose(T v1, T v2) {
        return /* ... */ ? v1 : v2;
    }
}

// 타입 인자 명시
String str = A.<String>choose("Korean", "Foreigner");

// 타입 인자 추론 (생략 가능)
int num = A.choose(1, 2);
```

**Kotlin 예시:**

```kotlin
fun <T> choose(v1: T, v2: T): T = if (/* ... */) v1 else v2

// 타입 인자 추론
val str: String = choose("Korean", "Foreigner")
val num: Int = choose(1, 2)

// 타입 인자 명시 (필요시)
val str2 = choose<String>("Korean", "Foreigner")
```

### 제네릭 클래스

타입 매개변수를 가진 클래스를 정의하여, 클래스 내부에서 사용되는 데이터 타입을 유연하게 만들 수 있습니다.

**Java 예시:**

```java
class Chooser<T> {
    T v1;
    T v2;

    T choose() {
        print(this.v1); print(this.v2);
        int input = readInt();
        return (input == 0) ? this.v1 : this.v2;
    }
}

// 타입 인자를 명시하여 사용
Chooser<Integer> c = new Chooser<>(1, 2);
Integer v = c.choose();
```

**Kotlin 예시:**

```kotlin
class Chooser<T>(
    val v1: T,
    val v2: T
) {
    fun choose(): T {
        print(v1); print(v2)
        val input = readInt()
        return if (input == 0) v1 else v2
    }
}

val c = Chooser<Int>(1, 2)
val v: Int = c.choose()
```

### 제네릭 타입 (리스트와 맵)

**Java 예시:**

```java
List<Integer> l = List.of(1, 2, 3);
Map<Integer, String> m = Map.of(1, "one", 2, "two");
```

**Kotlin 예시:**

```kotlin
val l: List<Int> = listOf(1, 2, 3)
val m: Map<Int, String> = mapOf(1 to "one", 2 to "two")
```

### 제네릭 클래스와 상속

**추상 클래스를 통한 공통 인터페이스 정의**

**Java 예시:**

```java
// 추상 클래스
abstract class List<T> {
    abstract T get(int idx);
}

// 구현 클래스들
class ArrayList<T> extends List<T> {
    T get(int idx) { /* ... */ }
}

class Bitvector extends List<Boolean> {
    Boolean get(int idx) { /* ... */ }
}

// 사용
List<String> l1 = new ArrayList<String>();
List<Boolean> l2 = new ArrayList<Boolean>();
List<Boolean> l3 = new Bitvector();
```

**Kotlin 예시:**

```kotlin
abstract class List<T> {
    abstract fun get(idx: Int): T
}

class ArrayList<T> : List<T>() {
    override fun get(idx: Int): T = /* ... */
}

class Bitvector : List<Boolean>() {
    override fun get(idx: Int): Boolean = /* ... */
}

val l1: List<String> = ArrayList<String>()
val l2: List<Boolean> = ArrayList<Boolean>()
val l3: List<Boolean> = Bitvector()
```

### 타입 매개변수 제한 (Upper Bounding)

제네릭 타입 T가 **특정 타입의 서브타입**이어야 함을 명시하여, 해당 타입이 제공하는 메서드나 필드를 안전하게 사용할 수 있게 합니다.

#### 문제 상황

```java
class Person {
    int age;
}
class Student extends Person {}

Person elder(Person p, Person q) {
    return (p.age >= q.age) ? p : q;
}

// ❌ 문제: elder()가 Person을 반환하므로, Student로 사용 불가
Student s = elder(new Student(), new Student()); // 컴파일 에러!
```

#### 해결: 타입 매개변수 제한

**Java 예시:**

```java
class A {
    // T는 반드시 Person의 서브타입이어야 함
    static <T extends Person> T elder(T p, T q) {
        return (p.age >= q.age) ? p : q;
    }
}

// ✅ 반환 타입이 Student로 추론됨
Student s = A.elder(new Student(), new Student());
```

**Kotlin 예시:**

```kotlin
fun <T : Person> elder(p: T, q: T): T {
    return if (p.age >= q.age) p else q
}

val s: Student = elder(Student(), Student())
```

**언어별 문법 비교:**

| Scala | Java | Kotlin |
|-------|------|--------|
| `<T <: Person>` | `<T extends Person>` | `<T : Person>` |

### 재귀적 타입 매개변수 제한

타입 매개변수가 **자기 자신을 포함하는 제네릭 타입**으로 제한될 때 사용됩니다.

#### 사용 예시: 정렬 가능한 리스트

`Comparable<T>`는 자신과 동일한 타입 T의 값과 비교될 수 있는 객체를 나타냅니다.

**Java 예시:**

```java
abstract class Comparable<T> {
    abstract boolean gt(T that);
}

class A {
    // T는 T와 비교 가능한 Comparable<T>의 서브타입이어야 함
    static <T extends Comparable<T>> void sort(List<T> lst) {
        // T 타입 원소에 대해 T 타입 인자를 받는 gt() 호출이 안전해짐
        if (lst.get(i).gt(lst.get(j))) {
            /* ... */
        }
    }
}

class Person extends Comparable<Person> {
    int age;
    boolean gt(Person that) {
        return this.age > that.age;
    }
}

List<Person> people = List.of();
A.<Person>sort(people);
```

**Kotlin 예시:**

```kotlin
abstract class Comparable<T> {
    abstract fun gt(that: T): Boolean
}

fun <T : Comparable<T>> sort(lst: List<T>): Unit {
    if (lst[i].gt(lst[j])) {
        /* ... */
    }
}

class Person(val age: Int) : Comparable<Person>() {
    override fun gt(that: Person): Boolean = this.age > that.age
}

val people: List<Person> = listOf()
sort<Person>(people)
```

---

## 3. 가변성 (Variance)

**가변성**은 상속과 제네릭이 결합될 때 발생하는 **제네릭 타입 사이의 서브타입 관계**를 정의하는 기능입니다.

### 가변성의 개념

`Student`가 `Person`의 서브타입일 때 (`Student <: Person`):

| 가변성 종류 | 관계 | 예시 | 설명 |
|------------|------|------|------|
| **공변 (Covariance)** | `B <: A` ⇒ `G<B> <: G<A>` | `List<Student> <: List<Person>` | 타입 인자와 같은 방향 |
| **불변 (Invariance)** | `B <: A` ⇏ 관계 없음 | `MutableList<Student>` ≠ `MutableList<Person>` | 서브타입 관계 없음 |
| **반변 (Contravariance)** | `B <: A` ⇒ `G<A> <: G<B>` | `Consumer<Person> <: Consumer<Student>` | 타입 인자와 반대 방향 |

### 문제 상황: averageAge 함수

```java
class Person { int age; }
class Student extends Person {}

int averageAge(List<Person> people) {
    int sum = 0;
    for (int i = 0; i < people.length(); i++) {
        sum += people.get(i).age;
    }
    return sum / people.length();
}

List<Person> people = /* ... */;
averageAge(people); // ✅ 통과

List<Student> students = /* ... */;
averageAge(students); // ❌ 타입 에러!
```

💡 **문제**: `List<Student>`는 `List<Person>`의 서브타입이 아니므로 타입 검사를 통과하지 못합니다.

### 가변성 판단 규칙

**PECS 원칙**: Producer Extends, Consumer Super

| 제네릭 타입 G | T 출력 사용 | T 입력 사용 | 가변성 | 예시 |
|-------------|------------|------------|-------|------|
| `List1<T>` (읽기 전용) | ✅ | ❌ | 공변 | `List<Student> <: List<Person>` |
| `List2<T>` (읽기/쓰기) | ✅ | ✅ | 불변 | 관계 없음 |
| `Int => T` (결과 타입) | ✅ | ❌ | 공변 | `(Int) -> Student <: (Int) -> Person` |
| `T => Int` (매개변수 타입) | ❌ | ✅ | 반변 | `(Person) -> Int <: (Student) -> Int` |

💡 **핵심**: G가 T를 **출력(Producer)**에만 사용하면 공변, **입력(Consumer)**에만 사용하면 반변, 둘 다 사용하면 불변입니다.

### 정의 시점 가변성 (Kotlin)

코틀린은 제네릭 타입 **정의 시점**에서 가변성을 지정합니다.

```kotlin
open class Person
class Student : Person()

// out: T를 출력(결과)에만 사용. 공변성 허용
class ReadOnlyList<out T> {
    fun get(idx: Int): T = /* ... */
    // fun add(t: T) // ❌ 불가능: out은 입력에 사용 불가
}

val l: ReadOnlyList<Person> = ReadOnlyList<Student>() // ✅ 허용됨

// in: T를 입력(매개변수)에만 사용. 반변성 허용
class Consumer<in T> {
    fun process(t: T): Unit = /* ... */
    // fun get(): T // ❌ 불가능: in은 출력에 사용 불가
}

val m: Consumer<Student> = Consumer<Person>() // ✅ 허용됨

// 맵의 경우: 키는 입력(in), 값은 출력(out)
class Map<in K, out V> {
    fun get(key: K): V = /* ... */
    fun add(key: K, value: V): Unit = /* ... */
}
```

### 사용 시점 가변성 (Java & Kotlin)

자바는 제네릭 타입을 **사용할 때** 가변성을 지정합니다. 모든 제네릭 타입은 기본적으로 불변으로 정의됩니다.

**Java 예시:**

```java
class Person { int age; }
class Student extends Person {}

class List<T> {
    int length() { /* ... */ }
    T get(int idx) { /* ... */ }
    void add(T t) { /* ... */ }
}

// List<? extends Person>: Person의 모든 서브타입에 대해 공변성 허용 (Producer)
int averageAge(List<? extends Person> people) {
    // ✅ people에서 데이터를 읽을(get) 수 있음
    int age = people.get(0).age;

    // ❌ people에 추가(add)는 불가능
    // people.add(new Student()); // 컴파일 에러

    return /* ... */;
}

// ✅ List<Student> 객체를 List<? extends Person> 자리에 전달 가능
averageAge(new List<Student>());

// List<? super Student>: Student의 모든 슈퍼타입에 대해 반변성 허용 (Consumer)
void addStudent(List<? super Student> students) {
    // ✅ students에 Student 객체를 추가(add)할 수 있음
    students.add(new Student(20));

    // ⚠️ 읽은(get) 데이터는 Object 타입으로만 처리 가능
    Object obj = students.get(0);
}

// ✅ List<Person> 객체를 List<? super Student> 자리에 전달 가능
addStudent(new List<Person>());
```

**Kotlin 사용 시점 가변성:**

```kotlin
open class Person(val age: Int)
class Student(age: Int) : Person(age)

class List<T> {
    fun length(): Int = /* ... */
    fun get(idx: Int): T = /* ... */
    fun add(t: T): Unit = /* ... */
}

fun averageAge(people: List<out Person>): Int {
    val age = people[0].age
    return /* ... */
}

fun addStudent(students: List<in Student>): Unit {
    students.add(Student(20))
}

averageAge(List<Student>())
addStudent(List<Person>())
```

💡 **참고**: 정의 할 때 가변성을 공변이나 반변으로 정한 경우, 사용 시에는 가변성을 바꿀 수 없습니다. 즉, 불변으로 정의한 제네릭 클래스에 한해 사용할 때 `in`이나 `out`을 붙여 가변성을 지정할 수 있습니다.

---

## 4. 오버로딩과 오버라이딩

### 메서드 오버로딩 (Overloading)

하나의 클래스 내에서 **메서드 이름은 같지만** 매개변수 타입이나 개수가 다른 메서드를 여러 개 정의하는 기능입니다.

**Java 예시:**

```java
class Cell {
    void write(String str) { /* ... */ }
    void write(int num) { /* ... */ }
}

Cell c = new Cell();
c.write("Hello"); // String 버전 호출
c.write(42);      // int 버전 호출
```

**Kotlin 예시:**

```kotlin
class Cell {
    fun write(str: String): Unit { /* ... */ }
    fun write(num: Int): Unit { /* ... */ }
}

val c = Cell()
c.write("Hello") // String 버전 호출
c.write(42)      // Int 버전 호출
```

### 메서드 오버라이딩 (Overriding)

부모 클래스를 상속받아 자식 클래스에서 **부모의 메서드와 시그니처가 완전히 동일한** 메서드를 재정의하는 기능입니다.

⚠️ **Kotlin**: `open` 및 `override` 키워드 필수

**Java 예시:**

```java
class Vector {
    int length() { /* 일반 구현 */ }
}

class SparseVector extends Vector {
    @Override
    int length() { /* 희소 행렬 최적화 */ }
}

Vector v = new SparseVector();
v.length(); // SparseVector의 length() 호출 (동적 디스패치)
```

**Kotlin 예시:**

```kotlin
open class Vector {
    open fun length(): Int { /* 일반 구현 */ }
}

class SparseVector : Vector() {
    override fun length(): Int { /* 희소 행렬 최적화 */ }
}

val v: Vector = SparseVector()
v.length() // SparseVector의 length() 호출
```

💡 **Kotlin 특징**: 자식 클래스에서 오버라이딩하는 것을 허용하려면 메서드에는 `open` 키워드를 붙여야 하고, 오버라이딩 할 때는 메서드에 `override` 키워드를 붙여야 합니다.

### 메서드 선택 규칙

메서드 호출 시 어떤 메서드를 실행할지 결정하는 규칙입니다.

1. **인자 타입에 맞는 메서드를 고릅니다**
2. **인자 타입에 맞는 메서드가 여럿이면, 인자의 타입에 가장 특화된 메서드를 고릅니다**
3. **메서드를 고를 때 인자의 정적 타입을 고려합니다**
4. **메서드를 고를 때 수신자(메서드를 호출하는 객체)의 동적 타입을 고려합니다** (오버라이딩)

### 동적 디스패치 (Dynamic Dispatch)

**수신자의 동적 타입**을 고려하여 메서드를 선택합니다. 이는 객체 지향 언어의 핵심 기능입니다.

```java
class Vector {
    int length() { /* 일반 구현 */ }
}

class SparseVector extends Vector {
    @Override
    int length() { /* 희소 행렬 최적화 */ }
}

Vector v = new SparseVector(); // 정적 타입: Vector, 동적 타입: SparseVector
v.length(); // ✅ v의 동적 타입인 SparseVector의 length() 호출
```

### 메서드 선택의 한계: 인자의 동적 타입 문제

⚠️ **중요**: 메서드 선택 시 **수신자의 동적 타입**은 고려하지만, **인자의 동적 타입**은 고려하지 않습니다 (대부분의 언어에서).

```java
class Vector {
    // 인자로 넘어온 that의 실제 동적 타입(SparseVector인지 Vector인지)을
    // 고려하지 않고 정적 타입인 Vector로만 처리합니다.
    Vector add(Vector that) {
        // ⚠️ that이 SparseVector여도 Vector처럼 처리
        // this.entries[i] + that.entries[i] ...
        // 성능 최적화 기회를 놓칠 수 있음
        return this;
    }
}
```

💡 **해결책**: 이런 한계를 극복하기 위해 **더블 디스패치(Double Dispatch)**와 같은 복잡한 패턴이 필요해지는 경우가 있습니다.

---

## 정리

### 핵심 개념 요약

| 개념 | 핵심 내용 | 주요 활용 |
|------|----------|----------|
| **서브타입 다형성** | 상속 관계를 통한 타입 호환 | 인터페이스, 추상 클래스 |
| **매개변수 다형성** | 제네릭을 통한 타입 추상화 | 컬렉션, 범용 알고리즘 |
| **가변성** | 제네릭 타입 간 서브타입 관계 | API 설계, 타입 안전성 |
| **오버로딩/오버라이딩** | 메서드 이름 재사용 | 편의성, 다형적 동작 |

### 언어별 주요 차이점

| 기능 | Java | Kotlin |
|------|------|--------|
| **가변성 지정** | 사용 시점 (`? extends`, `? super`) | 정의 시점 (`out`, `in`) + 사용 시점 |
| **최대 타입** | `Object` | `Any`, `Any?` |
| **오버라이딩** | `@Override` (선택) | `open`, `override` (필수) |
| **타입 추론** | 제한적 | 강력함 |
| **타입 매개변수 제한** | `<T extends Person>` | `<T : Person>` |

---

타입 시스템과 다형성을 올바르게 이해하고 활용하면, **견고하면서도 유연한** 코드를 작성할 수 있습니다. 각 언어의 특성을 이해하고 적절한 다형성 기법을 선택하는 것이 중요합니다.
